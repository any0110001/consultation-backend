/**
 * Dummy generation of patient summary based on result
 * @param result
 * @returns {string}
 */
export const generateSummary = (result) => {
  // Integration with LLMs can be implemented to intelligently generate summary
  return "Dummy LLM generated patient summary\n"
}
